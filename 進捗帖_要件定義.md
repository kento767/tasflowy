# 進捗帖 — 要件定義書

個人用タスク管理Webアプリ。コンセプトは「毎朝開いて、今日やるべきことが一目でわかる」こと。
この文書は**決定事項**と**裁量に委ねる部分**を分けて書いている。決定事項は守ること。それ以外の細部（コンポーネント設計、ライブラリ選定、細かいUX）は実装者の判断で最適化してよい。

---

## 1. 技術スタック（決定事項)

- フロントエンド: Next.js（App Router推奨だが裁量可）
- DB / 認証: Supabase（PostgreSQL + Supabase Auth)
- デプロイ: Vercel（GitHubリポジトリと連携し、pushで自動デプロイ）
- PWA対応: スマホのホーム画面に追加してアプリのように使えること
- 利用者は当面1人（本人のみ）。認証はシンプルでよいが、Supabase RLSで自分のデータのみアクセス可能にすること

## 2. コアコンセプト（決定事項）

- タスクには2種類ある: **やること（must）** と **やりたいこと（want）**
- ホーム画面は1本のリストで、色分け（または文字ラベル）で must / want を識別できる
- 完了したタスクはホームから消え、完了リストに移動する
- タスクをタップすると詳細が開き、サブタスク・期限・目印（フラグ）・メモを管理できる

## 3. データモデル（決定事項）

サブタスクは別テーブル。階層は1段まで（サブタスクの下に孫は作らない）。

```
tasks
  id            uuid PK
  user_id       uuid (auth.users FK)
  title         text
  type          'must' | 'want'
  due_date      date NULL          -- 期限なしを明示的に許す
  flagged       boolean
  note          text
  status        'active' | 'done'
  repeat        NULL | 'daily' | 'weekly' | 'monthly'  -- 拡張余地あり
  origin_task_id uuid NULL FK → tasks (ON DELETE SET NULL)  -- 昇格元の親
  created_at    timestamptz
  completed_at  timestamptz NULL

subtasks
  id              uuid PK
  task_id         uuid FK → tasks (ON DELETE CASCADE)
  title           text
  done            boolean
  completed_at    timestamptz NULL
  sort_order      int
  linked_task_id  uuid NULL FK → tasks (ON DELETE SET NULL)  -- 昇格先タスクへの参照
```

- 進捗はフィールドとして持たず、サブタスクの完了数から導出する（3/5完了 = 60%）
- サブタスクがないタスクは進捗表示なし（完了/未完了のみ）

## 4. 機能要件

### 4.1 ホーム画面（決定事項）

上から次の順に表示する:

1. **期限切れ**（目立つ色で警告）
2. **今日**（今日が締め切りのタスク）
3. **フラグ付き**
4. **それ以外**（期限が近い順。期限なしは最後）

- must / want は行の左端の色帯などで識別（詳細は裁量）
- 完了操作はホームから直接できること（ボタン or スワイプ、裁量）

### 4.2 詳細画面（決定事項）

- サブタスクの追加・チェック・並び替え・削除
- 期限、フラグ、メモ、typeの編集
- 完了サブタスクの表示ルール:
  - 同じチェックリスト内で未完了が上、完了済みが下に沈む
  - 間に「完了済み n件」の区切りを置き、タップで折りたたみ可能
  - 完了済みは薄色＋取り消し線、右端に完了日を小さく表示
  - 削除せず記録として残す

### 4.3 サブタスクの昇格（決定事項）

- サブタスクを独立タスクに昇格できる（長押しやメニューから。操作方法は裁量）
- 昇格処理（1トランザクション）:
  1. 新タスクをINSERT。title と type は親から引き継ぎ、due_date は NULL。origin_task_id に親のidを記録
  2. 元のサブタスクの linked_task_id に新タスクのidをセット
- 参照になったサブタスクの挙動:
  - 完了状態は自分で持たず、リンク先タスクの status から導出して表示
  - 手動チェック不可。「→」等のアイコンを付け、タップでリンク先の詳細へ遷移
  - 親の進捗計算には含まれる
- 昇格先タスク側は、詳細画面に「〇〇から昇格」と出自を小さく表示し、タップで親に戻れる
- 昇格先が削除されたら linked_task_id が NULL に戻り、通常の手動チェック式サブタスクに復帰する

### 4.4 タスクの完了（決定事項）

- 未完了のサブタスク（参照サブタスク含む）が残った状態でも親を完了できるが、確認を一度挟む。残りは未チェックのまま保存
- 完了時に completed_at を記録
- repeat が設定されたタスクは、完了時に次回分を自動生成する

### 4.5 完了リスト（決定事項）

- 完了日の新しい順に表示
- タップで詳細を開け、サブタスクの記録（何をいつ終えたか）を確認できる
- 完了リストに載るのは親タスクのみ。サブタスク単体は載せない

## 5. デザイン方針（決定事項）

- **極力シンプル**。装飾より視認性と操作の速さを優先する
- **禁止事項**: グラデーション付きのアイコンやボタン、AIアプリにありがちな派手な装飾（グラデーション背景、光彩、絵文字の多用、紫〜青のテック系グラデ配色）
- 色は must / want の識別と期限切れの警告など、意味のある箇所にだけ使う
- フォントはシステムフォントでよい
- ダークモードは必須ではない（余裕があれば）

## 6. 裁量に委ねる部分

以下は実装者が使いやすさを優先して自由に決めてよい:

- コンポーネント設計、状態管理、フォルダ構成
- UIライブラリの採用有無（採用する場合もデザイン方針に従うこと）
- 追加ボタンの配置、スワイプ操作の詳細、アニメーションの有無
- 並び替えの手動調整機能の有無
- repeat の詳細仕様（曜日指定など）の拡張
- オフライン時の挙動
- その他、コンセプト「毎朝見て今日やるべきことがわかる」に資する小さな改善

## 7. 最初にやらないこと（スコープ外）

- タグ、プロジェクト分類、共有・コラボ機能、コメント
- プッシュ通知
- 統計・振り返り画面（ただし completed_at を記録しているので将来追加可能な設計にする）
